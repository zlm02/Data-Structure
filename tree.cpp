#include <stdio.h>
/*
 *一、 树相关基本概念
结点：树中独立的单元。
节点的度：结点拥有子树数目称为结点的度。
树的度：树内个结点的最大值。
叶子：度为0的结点。
层次：结点的层次从根开始定义，根为第一层，根孩子为第二层，以此类推。

 二、 二叉树
1. i层最多有2^(i-1)个节点。
2.深度为k的二叉树最多2^k -1个节点。
3.非空二叉树，叶子结点n0.度为2的结点数目为n2，no=n2+1.

完全二叉树：先有左子树再有右子树，先上层铺满在铺下层。
*/

